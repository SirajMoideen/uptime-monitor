name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Check Environment
        run: |
          whoami
          pwd
          docker --version
          kubectl get nodes